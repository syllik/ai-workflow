{
  "name": "ai-workflow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.21.0"
  },
  "scripts": {
    "test": "node --test",
    "verify": "npm test && node scripts/workspace/cli.mjs check --manifest-only && node scripts/workspace/cli.mjs check && node scripts/workspace/rollout-ci.mjs source && node scripts/workspace/ci-activation.mjs && git diff --check"
  },
  "dependencies": {
    "yaml": "2.9.0"
  }
}
