# AI profile entry

This profile routes AI work through the GitHub-rooted workspace contract.

`AI.md → FLOW.md → one workspace.yaml record → role rules → target AGENTS.md/context → relevant decisions/files`

Read only the selected target repository. GitHub records are authoritative; legacy central project contexts are migration-only and repository auto-discovery is forbidden. Sol plans and hands off one prompt. Luna executes bounded work and stops at `IMPLEMENTATION_COMPLETE` or `BLOCKED`; independent review is performed separately by Sol 5.6 High.
