# Project context migration

The active project registry is `workspace.yaml`, rendered into `projects/index.md`. Target repositories own their `.ai/context.md`, `AGENTS.md`, and relevant decisions.

Files under this directory are preserved migration history only. Agents must not use legacy central contexts as the active reading path or auto-discover repositories.
